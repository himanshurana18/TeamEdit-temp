/**
 * Type definitions for media device configuration.
 * Includes:
 * - Device ID and label
 * - WebRTC device types
 *
 * By Himanshu rana .
 */

export interface MediaDevice {
  deviceId: string;
  label: string;
}
